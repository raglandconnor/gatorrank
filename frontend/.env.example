# API BASE URL for calling backend API (FastAPI). No trailing slash.
# Local dev: http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend CORS: set backend/.env CORS_ORIGINS to include this app's origin
# (e.g. http://localhost:3000) so browser requests from the Next app succeed.
